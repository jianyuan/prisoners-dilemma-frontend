= simple_form_for(@submission) do |f|
  = f.error_notification

  .form-inputs
    = f.association :user
    = f.association :game_round
    = f.input :name
    = f.input :code

  .form-actions
    = f.button :submit
